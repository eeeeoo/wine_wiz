<% @categories.each do |category| %>

<div class="row">
  <div class="col"><%= category.name %></div>
  <!-- <div class="col"><%= category.name %></div>
  <div class="col"><%= category.name %></div>
  <div class="col"><%= category.name %></div>
  <div class="col"><%= category.name %></div>
  <div class="col"><%= category.name %></div>
  <div class="col"><%= category.name %></div> -->
</div>

<% end %>

<!-- <% @categories.each do |category| %>
<li><%= category.name %></li>
<% end %> -->
